<odoo>
    <data>
      <!-- explicit list view definition -->
  <!--
      <record model="ir.ui.view" id="parcel.list">
        <field name="name">parcel list</field>
        <field name="model">parcel.parcel</field>
        <field name="arch" type="xml">
          <tree>
            <field name="name"/>
            <field name="value"/>
            <field name="value2"/>
          </tree>
        </field>
      </record>
  -->
  
      <!-- actions opening views on models -->
  <!--
      <record model="ir.actions.act_window" id="parcel.action_window">
        <field name="name">parcel window</field>
        <field name="res_model">parcel.parcel</field>
        <field name="view_mode">tree,form</field>
      </record>
  -->
  
      <!-- server action to the one above -->
  <!--
      <record model="ir.actions.server" id="parcel.action_server">
        <field name="name">parcel server</field>
        <field name="model_id" ref="model_parcel_parcel"/>
        <field name="state">code</field>
        <field name="code">
          action = {
            "type": "ir.actions.act_window",
            "view_mode": "tree,form",
            "res_model": model._name,
          }
        </field>
      </record>
  -->
  
      <!-- Top menu item -->
  <!--
      <menuitem name="parcel" id="parcel.menu_root"/>
  -->
      <!-- menu categories -->
  <!--
      <menuitem name="Menu 1" id="parcel.menu_1" parent="parcel.menu_root"/>
      <menuitem name="Menu 2" id="parcel.menu_2" parent="parcel.menu_root"/>
  -->
      <!-- actions -->
  <!--
      <menuitem name="List" id="parcel.menu_1_list" parent="parcel.menu_1"
                action="parcel.action_window"/>
      <menuitem name="Server to list" id="parcel" parent="parcel.menu_2"
                action="parcel.action_server"/>
  -->
    </data>
  </odoo>
  